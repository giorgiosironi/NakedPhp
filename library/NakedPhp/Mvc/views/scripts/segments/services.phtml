<?php
$labels = array();
foreach ($this->services as $index => $object) {
    $label = $this->displayObject($object);
    $url = $this->url(array('action' => 'view', 'type' => 'service', 'object' => $index));
    $labels[] = "<a href=\"$url\">$label</a>";
}
echo implode('; ', $labels);
